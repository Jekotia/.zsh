Personalised, highly-portable zsh environment.
If you find this on a server and need to inquire, ask Josh.

Install with:
 > curl -L https://github.com/Jekotia/.zsh/raw/master/shell-setup.bash | sudo bash -s $USER
